'use strict';

/**
 * MembersController
 *
 */
app.lazy.controller('HomeController', ['$scope','$routeParams','$filter',
	function ($scope, $routeParams, $filter) {

		var self = this;

		// console.log($scope.auth);

	}
]);